# results/blast/

BLAST output for Steps 2 and 4.

- `tblastn_<database>_hits.csv` and a screenshot of the top hits (Step 2)
- `tblastn_selected_alignment.txt`: pairwise alignment of the selected hit
- `blastp_nr_hits.csv` and a screenshot of the top hits (Step 4)
